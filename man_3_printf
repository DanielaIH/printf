.\" Manpage for _printf.
.\" Contact https://github.com/DanielaIH and https://github.com/Robert-octavo to report an error.
.TH _PRINTF 3 "25 March 2022" "1.0" "_PRINTF MAN PAGE"
.SH NAME
.B	_printf
.SH SYNOPSIS
.B	#include "main.h"
.B 	int _printf(const char *format, ... );

.SH DESCRIPTION
.B _printf()
	The _printf() function produces output according to a format as described below.   The  function
	prints the ARGUMENTS from a function like output to stdout, the standard output stream.

.SH CONVERSION SPECIFIERS
    	A character that specifies the type of conversion to be applied.  The conversion specifiers and
	their meanings are:

	 c     print characters
	 s     print strings
	d,i    print integers or floats numbers of max and min size of an int type
	 b     print unsigned integer as binary (base 2)
	 u     print unsigned integer
	 o     print a number in octal
	 x     print a number in hexadecimal format
	 X     print a number in hexadecimal format
	 r     print a string in reverse
	 R     print the rot13'ed string

.SH EXAMPLES

	The _printf() function is called as next: _printf("This is an example to print a %c.", 's');
	the output will be >>This is an example to print a s.

.SH RETURN VALUE
    	Upon  successful return, _printf() function returns the number of characters printed (excluding the
	null byte used to end output to strings).

.SH ERROR
	On failures, the _printf() function returns (-1).

.SH AUTHORS
Daniela Ibarra (https://github.com/DanielaIH) Robert Octavo (https://github.com/Robert-octavo)
